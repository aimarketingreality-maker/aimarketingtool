
import { colors } from '@/lib/theme';

export default function VisualBuilder() {
  return (
    <div className={`${colors.background.secondary} min-h-screen`}>
      <p>Visual Builder</p>
    </div>
  );
}
